import { ParseContentOrderPipe } from './parse-content-order.pipe';

describe('ParseContentOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseContentOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
